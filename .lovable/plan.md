Trocar a referência do logo Velara em `src/pages/pdv/evaluations/EvaluationsArte.tsx`:

- Adicionar import: `import velaraLogo from "@/assets/logo_velara_preto.png";`
- Substituir o `src="/logo_velara_preto.png"` por `src={velaraLogo}` na assinatura "powered by" do rodapé.

Sem outras alterações.